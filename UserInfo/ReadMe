编译问题及其解决办法：

Q:找不到<ft2build.h>
A:默认的ft2build.h文件和freetype文件夹在/usr/include/freetype2/文件夹内，请将它们放在/usr/include/目录下，因为这是默认的系统目录。
如果您的系统中没有freetype2库,请在网络上下载并安装。freetype库依赖libpng和zstd等开源库,请记得先下载安装它们。


启动方法：
在run.sh所在目录，起一个终端，在终端输入：
./run.sh


调试方法：
Q：怎么修改分辨率？
A：在根目录下的congfig.json文件中您可以修改窗口的宽高；修改后需要重新启动程序生效

Q：xml放在哪里？
A：将xml文件放在frame/UI/res/Form目录下，注意只会读取一个文件，所以您每次调试只能放一个xml文件。

Q：资源放在哪里？
A：要调试的xml使用的所有的资源都请放在frame/UI/res/目录下即可。放置内容越少，运行速度越快。

Q：SC图片放在那里？
A：要调试的xml使用的SC图片请您放在frame/UI/res/SC目录下

Q：如何调试？
A：启动应用后，会读取xml文件内容，将元器件内容描画到窗口上。并显示位置信息。
	1.压下鼠标左键将只显示鼠标位置的actor及其位置信息，松开后复原。
	2.压下鼠标右键，将半透显示xml布局以便和下面的SC图片对比，松开后复原
	3.在actor1范围内压下鼠标左键后，移动到actor1范围外松开；在actor2范围内再次按压鼠标左键，将显示actor1和actor2的位置信息。
	同理可显示想要的部分布局。
	4.修改xml文件并保存后，应用会即时更新xml布局。如果想替换资源，需要在替换资源后，修改下xml并保存，或者重新启动应用才能刷新